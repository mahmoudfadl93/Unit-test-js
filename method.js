/*
beforeAll(fn,timeout)
beforeEach(fn,timeout)
*/

beforeAll(() => {
  //Connect to Database
  //Empty testing table
  //Add Dummy Data For Teating
  //Prepare Everything before Testing
});

aftereAll(() => {
  //Clean Database
  //Clean Cache
});
